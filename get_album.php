<?php
// $album = $_GET['album'] ?? '';
// $folder = __DIR__ . "/assets/img/" . $album;

// $files = [];
// if (is_dir($folder)) {
//     foreach (scandir($folder) as $file) {
//         if (
//             preg_match('/\.(jpg|jpeg|png|gif)$/i', $file) && 
//             strtolower($file) !== 'cover.png' // exclude cover.png
//         ) {
//             $files[] = $file;
//         }
//     }
// }

// header('Content-Type: application/json');
// echo json_encode($files);
// exit;
?>

